#include <stdio.h>
int main()
{
 int a; 
    while(true)
   {
       scanf("%d", &a);
            if(a==0){
             pritnf(" friend's name is fool");
            }else{
             printf(" I am fool");
            }
    }
return 0;
}
